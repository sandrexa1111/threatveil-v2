{
  "name": "threatveil",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "build:frontend": "cd frontend && npm run build",
    "dev:backend": "cd backend && uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000"
  }
}
